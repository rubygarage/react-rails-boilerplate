en:
  errors:
    admin:
      invalid: Invalid email or password.
    rules:
      user:
        unique?: is not unique
        eql?: doesn't match
        confirmed?: email is not confirmed
        credentials_valid?: invalid credentials
